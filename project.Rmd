---
title: "DMII"
author: "Ana Catarina Monteiro, Artur Ferreira, Lucas Parada"
date: "4/25/2020"
output: html_document


---
```{python}
# movie = pd.read_csv('data/movie.txt', sep='\\', encoding='windows-1252')
# movie['moviename'] = movie['moviename,movieid'].str.split(pat=',').str[:-1]
# 
# movie['moviename'] = movie['moviename'].apply(lambda x: ','.join(x))
# movie['moviename'] = movie['moviename,movieid'].str.split(pat=',').str[:-1]
# movie['moviename'] = movie['moviename'].apply(lambda x: ','.join(x))
# movie[['movieid', 'moviename']].to_csv('data/movie.csv')
```


```{r setup, include=FALSE}
  
  # profile <- read.csv('data/profile.txt')
  # movie.name <- read.delim('data/movie-names.txt', sep="\t", encoding = "latin1")
  # movie <- read.delim('data/movie.txt', sep=",", encoding = "latin1")

  Ratings.timed <- read.csv('data/teste.csv')
  

  # movie.name <- read.table('data/Ratings.timed.txt', sep="\t", fileEncoding="latin1")
  
  
  


  

```
